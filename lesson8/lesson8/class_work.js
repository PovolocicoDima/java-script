// STRUCTURE DATA -> TEXT

1) XML(old) - tags / attributes , LOTS OF CODE,
2) JSON(new) + simple / less code , fast, object - FRONTEND
3) YAML + plain text, very simple, INDENTATION   - BACKEND

 * STORE
 * SEND
 * RECEIVE
 * IMPORT
 * EXPORT


 JSON.stingify( o )
 JSON.parse( " JSON TEXT ")   --> DATA
 JSON.stringify( data )  -->  " JSON text "
